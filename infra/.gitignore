# Local Terraform working dir + provider binaries
.terraform/
terraform/.terraform/

# State is stored remotely when configured; never commit local state
*.tfstate
*.tfstate.*

# Saved plans
tfplan
*.tfplan

# Local overrides (never commit)
*.tfvars
*.tfvars.json
!example.tfvars
