# Public SPA config (safe to commit; baked into the client bundle at build time).
#
# Local dev:
#   VITE_API_URL=http://localhost:5192
#
# Render builds set this in render.yaml / render.dev.yaml — do not rely on this file in CI.

VITE_API_URL=http://localhost:5192
